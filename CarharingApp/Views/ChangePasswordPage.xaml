<Window x:Class="CarharingApp.Views.ChangePasswordPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CarharingApp.Views"
        mc:Ignorable="d"
        Title="Смена пароля" Height="650" Width="500"
        Background="AliceBlue"
        ResizeMode="CanResize"
                WindowStartupLocation="CenterScreen">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Смена пароля"
                   FontSize="30" FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Foreground="Aquamarine"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Старый пароль" FontWeight="Bold" Foreground="Aquamarine"/>
            <PasswordBox Name="OldPasswordBox" Height="40" Padding="10" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="Новый пароль" FontWeight="Bold" Foreground="Aquamarine"/>
            <PasswordBox Name="NewPasswordBox" Height="40" Padding="10" FontSize="14"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,20">
            <TextBlock Text="Подтверждение" FontWeight="Bold" Foreground="Aquamarine"/>
            <PasswordBox Name="ConfirmPasswordBox" Height="40" Padding="10" FontSize="14"/>
        </StackPanel>

        <Button Grid.Row="4" 
                Content="Сменить пароль"
                Background="Beige" 
                Foreground="RosyBrown"
                Height="40"
                Margin="5,0,5,0"
                Click="ChangePasswordButton_Click"/>
        <Button Grid.Row="5"
                Name="Out"
                Content="Назад"
                Margin="5,10,5,0"
                Height="40"
                Background="Beige"
                Foreground="RosyBrown"
                Click="Out4"/>
    </Grid>
</Window>
